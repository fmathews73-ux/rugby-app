{
  "name": "rugby-app-monorepo",
  "version": "0.0.0-dev",
  "private": true,
  "description": "Rugby mobile app monorepo. Workspaces cover packages/* and services/*; mobile/ is intentionally NOT a workspace so Metro / Expo run against a self-contained node_modules.",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "typecheck:shared": "tsc --noEmit --project packages/shared/tsconfig.json",
    "typecheck:pipeline": "tsc --noEmit --project services/pipeline/tsconfig.json",
    "generate": "npm run generate --workspace=@rugby-app/pipeline"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "~5.6.3"
  }
}
