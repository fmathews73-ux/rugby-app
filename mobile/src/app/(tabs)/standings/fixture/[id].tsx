/**
 * The fixture drill, hosted INSIDE the Tables stack — so a match
 * opened from a team-in-competition drill pops back to that drill
 * (and then the standings), never sideways into the Fixtures tab.
 * Same screen component; only the host stack differs.
 */
export { default } from '../../fixtures/[id]';
