/**
 * The fixture drill, hosted INSIDE the Teams stack — so a match opened
 * from a team hub's Next/Last Match card pops back to that hub, never
 * sideways into the Fixtures tab. Same screen component; only the
 * host stack differs.
 */
export { default } from '../../fixtures/[id]';
