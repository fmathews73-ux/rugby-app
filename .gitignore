# --- secrets & env (never commit — see CLAUDE.md §1, §8) ---
.env
.env.*
*.local
*-key.json
*-credentials.json
service-account*.json

# --- node / js ---
node_modules/
npm-debug.log*
yarn-error.log*

# --- expo / react native (mobile app adds its own .gitignore too) ---
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.mobileprovision

# --- terraform (infra) ---
*.tfstate
*.tfstate.*
.terraform/
*.tfvars

# --- os / editor ---
.DS_Store
Thumbs.db
